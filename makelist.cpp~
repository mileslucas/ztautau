#include <fstream>

using namespace std;

void makelist() {

  fstream out;
  out.open("variable_info.txt");

  /* Import files */
  TFile *data_f = new TFile("emu_data.root");

  /* Get Trees */
  TTree *data = (TTree *) data_f->Get("h10");
  

}
